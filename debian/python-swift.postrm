#!/bin/sh                                                                                                                                 

set -e

if [ "${1}" = "purge" ] ; then
	rm -rf /var/cache/swift
	rm -rf /var/backups/swift
fi

#DEBHELPER#                                                                                                                                

exit 0
